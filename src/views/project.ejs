<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= project.title %> - Portfolio</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <nav class="navbar">
        <div class="container">
            <h1 class="logo">Portfolio</h1>
            <ul class="nav-links">
                <li><a href="/">홈</a></li>
                <li><a href="/#about">About</a></li>
                <li><a href="/#skills">Skills</a></li>
                <li><a href="/#projects">Projects</a></li>
                <li><a href="/#contact">Contact</a></li>
            </ul>
        </div>
    </nav>

    <section class="section">
        <div class="container">
            <div class="project-detail">
                <h1><%= project.title %></h1>
                <div class="project-image">
                    <img src="<%= project.image_url %>" alt="<%= project.title %>" onerror="this.src='/images/placeholder.jpg'">
                </div>
                <div class="project-info">
                    <h2>프로젝트 개요</h2>
                    <p><%= project.description %></p>

                    <h3>기술 스택</h3>
                    <p><%= project.technology %></p>

                    <% if (project.github_url || project.demo_url) { %>
                        <h3>프로젝트 링크</h3>
                        <div class="project-links">
                            <% if (project.github_url) { %>
                                <a href="<%= project.github_url %>" target="_blank" class="btn">GitHub 저장소</a>
                            <% } %>
                            <% if (project.demo_url) { %>
                                <a href="<%= project.demo_url %>" target="_blank" class="btn">라이브 데모</a>
                            <% } %>
                        </div>
                    <% } %>

                    <div class="project-links" style="margin-top: 3rem;">
                        <a href="/#projects" class="btn btn-secondary">프로젝트 목록으로</a>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <footer class="footer">
        <div class="container">
            <p>&copy; 2025 Portfolio. All rights reserved. Built with Node.js & Express</p>
        </div>
    </footer>
</body>
</html>
